py("event","order" ,{
    "id":" {{ orderNo }}",
    "money":"{{ money }}",
    "items":[
        {% for item in items %}
        {"id":"{{ item['id'] }}","count":"{{ item['count'] }}","price":"{{ item['price'] }}"} {% if !loop.last %},{% endif %}
        {% endfor %}
    ]
});

py('send','visit','order');
